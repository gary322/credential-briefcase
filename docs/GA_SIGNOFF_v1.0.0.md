# GA Sign-off: v1.0.0

This file records the required named approvals for cutting the `v1.0.0` GA release.

It is intentionally checked into the repo so the decision is:

- reviewable
- auditable
- attached to the exact source tree that was tagged

## Release

- Tag: `v1.0.0`
- Decision date (UTC): `REPLACE_ME_DECISION_DATE_UTC`

## Evidence Artifacts

Fill in the exact release artifacts attached to the GitHub Release (or equivalent):

- Release manifest: `REPLACE_ME_RELEASE_MANIFEST_ARTIFACT`
- SBOM (SPDX JSON): `REPLACE_ME_SBOM_ARTIFACT`
- Provider contract report: `REPLACE_ME_PROVIDER_CONTRACT_ARTIFACT`
- Security assessment bundle: `REPLACE_ME_SECURITY_ASSESSMENT_ARTIFACT`
- GA qualification bundle: `REPLACE_ME_GA_QUALIFICATION_ARTIFACT`
- External security review report: `REPLACE_ME_EXTERNAL_REVIEW_ARTIFACT`

## Approvals (Name + Date)

- Security: `REPLACE_ME_SECURITY_APPROVER` (`REPLACE_ME_SECURITY_DATE_UTC`)
- Interoperability: `REPLACE_ME_INTEROP_APPROVER` (`REPLACE_ME_INTEROP_DATE_UTC`)
- Operations/SRE: `REPLACE_ME_OPS_APPROVER` (`REPLACE_ME_OPS_DATE_UTC`)
- Release owner: `REPLACE_ME_RELEASE_OWNER` (`REPLACE_ME_RELEASE_OWNER_DATE_UTC`)

## Notes

Optional notes about residual risks, mitigations, or follow-ups:

- `REPLACE_ME_NOTES`
