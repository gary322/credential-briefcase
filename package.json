{
  "name": "agentic-auth-monorepo",
  "private": true,
  "version": "0.1.14",
  "license": "Apache-2.0",
  "packageManager": "pnpm@9.15.5",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "gen": "pnpm -r gen",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
